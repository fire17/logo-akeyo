# logo.akeyo.io

A one-page redirect. GitHub Pages allows a single custom domain per repository, so this
repo exists only to make **logo.akeyo.io** an alias of the real thing:

### → [tesseract.akeyo.io](https://tesseract.akeyo.io/) — [fire17/TesseractLogo](https://github.com/fire17/TesseractLogo)

The path is preserved, so `logo.akeyo.io/4d.html` lands on the 4D motion lab.
